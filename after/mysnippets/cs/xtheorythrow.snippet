public static IEnumerable<object[]> ${1:TheoryName} => new[]
{
    new object[] { A.Dummy<${~2}>()${+} }
};

[Theory, MemberData(nameof(${~1}))]
public void ${~1}_ExceptionThrown(${~2} ${3:input_name})
{
    /* Arrange */
    void shouldThrow() => new ${sut}(${~3})

    /* Act */
    var ex = Record.Exception(shouldThrow);

    /* Assert */
    Assert.NotNull(ex);
}
